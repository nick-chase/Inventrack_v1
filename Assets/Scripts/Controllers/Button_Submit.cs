using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

/*
 * Nicholas Chase
 * Reusable Button for sending Form Data for DB
 * 
 */

public class Button_Submit : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
